1.当目标与背景的颜色比较接近时，GrabCut 容易分割失败。因为 GrabCut 主要通过 GMM 颜色模型区分前景和背景，如果两者颜色分布相似，GMM 很难准确判断像素属于前景还是背景，容易出现背景被误分为前景、目标边缘缺失等问题。

2. ① 边缘先验：利用 Canny 等方法检测目标边缘，帮助确定分割边界。 
② 多次交互：用户手动标记一些确定的前景和背景，再进行第二次 GrabCut，提高分割准确率。 
③超像素预分割：先将颜色相近的像素组成小区域，再进行 GrabCut，减少噪声和错误分割。

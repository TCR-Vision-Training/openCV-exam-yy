实验结果（结合截图）：
直接使用 cv2.matchTemplate 匹配失败。最大相似度仅为 0.317，算法将目标错误地定位在了左下角的紫色柱状图上，未命中红五角星。
失效原因分析（三点）：
1.	旋转与缩放：场景中的五角星发生了旋转和缩放，而 matchTemplate 不具备旋转和尺度不变性，像素相对位置被打乱，导致匹配失效。
2.	严重遮挡：五角星中心被白色块遮挡，缺失了模板的核心特征，严重破坏了相似度计算。
3.	干扰误导：下方排列规则的柱状图和黑线，其局部相似度比受遮挡、旋转的目标更高，导致算法选错了位置。

